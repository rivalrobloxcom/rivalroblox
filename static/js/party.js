const POST_ID=new URLSearchParams(location.search).get('id') || window.POST_ID;
if(!POST_ID){document.addEventListener('DOMContentLoaded',()=>uiAlert('No party selected.','Missing party','⚠️').then(()=>location.href='find-players.html'));}
let lastCount=0;
async function loadParty(){let d=await api('/api/party/'+POST_ID);let p=d.post,me=d.me,mem=d.membership;$('#partyTitle').textContent=p.title;$('#partyDesc').textContent=p.description||'';$('#partyMode').textContent=p.game_mode||'';$('#partyRank').textContent=p.rank_requirement||'';$('#partyRegion').textContent=p.region||'';$('#partyCount').textContent=p.current_players+'/'+p.max_players;$('#members').innerHTML=d.members.map(m=>`<div class="member"><span class="avatar">${esc(m.avatar)}</span><div><b>${esc(m.username)}</b><small>${m.role}${me&&m.id===me.id?' • You':''} • ${esc(m.rank)}</small></div>${mem&&mem.role==='owner'&&m.id!==me.id?`<button onclick="kick(${m.id})">Kick</button>`:''}</div>`).join('');let isOwner=mem&&mem.role==='owner';let isMember=!!mem;$('#joinBtn').style.display=!isMember&&p.status==='active'?'block':'none';$('#joinBtn').textContent=p.current_players>=p.max_players?'Party Full':'Join Party';$('#joinBtn').disabled=p.current_players>=p.max_players;$('#leaveBtn').style.display=isMember&&!isOwner?'block':'none';$('#ownerControls').innerHTML=isOwner?'<button class="ghost danger" onclick="closeParty()">Close Party</button>':'';if(isMember){$('#chatLog').innerHTML=d.messages.map(m=>`<div class="msg ${me&&m.user_id===me.id?'me':''}"><b>${esc(m.username)}</b><p>${esc(m.body)}</p><small>${ago(m.created_at)}</small></div>`).join('');if(d.messages.length!==lastCount){$('#chatLog').scrollTop=$('#chatLog').scrollHeight;lastCount=d.messages.length}}else{$('#chatLog').innerHTML='<p class="notice">Join the party to view chat.</p>'}}
$('#joinBtn').onclick=async()=>{if($('#joinBtn').disabled)return uiAlert('This party is full. Try another party.','Party full','🚫');try{let r=await api('/api/party/'+POST_ID+'/join',{method:'POST'});location.href=(r.redirect||'').replace('/party/','party.html?id=').replace('/lfg','find-players.html')}catch(e){let msg=e.message==='Party is full'?'This party is full. Try another party.':e.message;uiAlert(msg,msg.includes('full')?'Party full':'Could not join',msg.includes('full')?'🚫':'⚠️')}};
$('#leaveBtn').onclick=async()=>{if(await uiConfirm('Leave this party? You will lose access to its chat.','Leave party','🚪')){try{let r=await api('/api/party/'+POST_ID+'/leave',{method:'POST'});location.href=(r.redirect||'').replace('/party/','party.html?id=').replace('/lfg','find-players.html')}catch(e){uiAlert(e.message,'Could not leave','⚠️')}}};
$('#send').onclick=async()=>{let body=$('#msg').value.trim();if(!body)return;try{await api('/api/party/'+POST_ID+'/messages',{method:'POST',body:JSON.stringify({body})});$('#msg').value='';loadParty();refreshBell()}catch(e){uiAlert(e.message,'Message not sent','💬')}};
$('#msg').addEventListener('keydown',e=>{if(e.key==='Enter')$('#send').click()});
async function kick(id){if(await uiConfirm('Kick this member from the party? They will lose chat access.','Kick member','🥾')){try{await api('/api/party/'+POST_ID+'/kick',{method:'POST',body:JSON.stringify({user_id:id})});uiAlert('Member kicked successfully.','Member removed','✅');loadParty()}catch(e){uiAlert(e.message,'Kick failed','⚠️')}}}
async function closeParty(){if(await uiConfirm('Close this party? It will disappear from Find Players and chat will lock.','Close party','🔒')){try{let r=await api('/api/party/'+POST_ID+'/close',{method:'POST'});location.href=(r.redirect||'').replace('/party/','party.html?id=').replace('/lfg','find-players.html')}catch(e){uiAlert(e.message,'Close failed','⚠️')}}}
loadParty();setInterval(loadParty,2500);
